# V2.3.3

## Pressure-screen layout

- The top row now shows `PRESSURE` on the left and current pressure on the right.
- Moved minimum, delta and maximum into a dedicated bottom row.
- Replaced the old delta glyph with a clear 7x7 outlined Greek delta.
- Added a two-pixel gap between `MIN:` / `MAX:` and their values.
- Expanded the chart vertically into the space freed below the header.
