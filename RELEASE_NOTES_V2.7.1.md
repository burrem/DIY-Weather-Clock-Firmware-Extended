# V2.7.1

## SNTP diagnostics

- Logs every configured SNTP server after a successful time synchronization.
- Shows its hostname when present, resolved IP address and lwIP reachability value.
- A DHCP-provided local/GPS NTP server can now be identified in `/log`.
