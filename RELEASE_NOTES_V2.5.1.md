# V2.5.1

## Live-screen orientation fix

- Rotated the web Canvas by 180 degrees to match the physical OLED's
  `setRotation(2)` configuration.
- The physical display and framebuffer data are unchanged.
